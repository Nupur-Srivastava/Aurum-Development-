import { FindLabelPipe } from './find-label.pipe';

describe('FindLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FindLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
